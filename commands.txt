# ansible
ansible all -m ping
ansible all -m shell -a "uptime"

# password for admin user on Harbor
export HARBOR_ADMIN=3kjDqy7TDs6iHr6IL

ansible-playbook registry-playbook.yml
ansible-playbook registry-playbook.yml -v

ansible-playbook kubernetes-playbook.yml
ansible-playbook kubernetes-playbook.yml -v

# ansible config precedence
ANSIBLE_CONFIG (environment variable if set)
ansible.cfg (in the current directory)
~/.ansible.cfg (in the home directory)
/etc/ansible/ansible.cfg

# SSH
ssh -i terraform/cncf_key root@$(terraform output do-registry-instance-ipv4-address)
ssh -i terraform/cncf_key -v root@$(terraform output do-registry-instance-ipv4-address)

ssh -i terraform/cncf_key root@$(terraform output do-registry-instance-ipv4-address) -a "apt-get update -yqq && apt-get install -yqq python-minimal"
ssh -i terraform/cncf_key root@$(terraform output do-registry-instance-ipv4-address) -v -a "apt-get update -yqq && apt-get install -yqq python-minimal"
